import { IG } from './interfaces/game';

export const Util = {
    AllPlayersHaveChosenAColor: function(G: IG){

    },
    Game: {
        IsOver: function(G: IG){
            return false;
        },
    }
}